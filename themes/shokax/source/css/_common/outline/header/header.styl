// Header Section
// --------------------------------------------------
#header {
  margin: 0 auto;
  position: relative;
  width: 100%;
  height: 50vh;
  text-shadow: 0.05rem 0.1rem 0.1rem rgba(0,0,0,0.2);
  color: var(--header-text-color);

  a:hover {
    color: currentColor;
  }
}

@import 'image';
@import 'nav';
@import 'brand';
@import 'tool';
@import 'waves';
